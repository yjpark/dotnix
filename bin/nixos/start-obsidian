#!/usr/bin/env bash

# Note for nixos fonts to be used, need to link them into ~/.local/share/fonts
# or link all system fonts with
#
# ln -s /run/current-system/sw/share/X11/fonts ~/.local/share/fonts
# 

cd /tmp
nohup flatpak run md.obsidian.Obsidian > /tmp/obsidian.log &

